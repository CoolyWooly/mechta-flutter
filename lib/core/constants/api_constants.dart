abstract class ApiConstants {
  static const String baseUrl = 'https://api.mechta.kz/';
  static const Duration connectTimeout = Duration(seconds: 15);
  static const Duration receiveTimeout = Duration(seconds: 15);
}
